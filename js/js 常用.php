===============================================================================
// 初始字符串
var str = ',,,,,1231313,,,,,,,';
// 进行替换
var newSrt = str.replace(/,/g,''); // 将字符串内所有的,替换掉
===============================================================================
$.trim($(this).val()); // 去掉字符串两端空格